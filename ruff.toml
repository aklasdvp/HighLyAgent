# HighLyAgent lint configuration.
line-length = 110
target-version = "py311"

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
# Tests deliberately set required environment variables before package imports.
"tests/*" = ["E402"]
