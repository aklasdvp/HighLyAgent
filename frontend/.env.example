# Copy to .env and adjust to your backend server.
#
#   cp .env.example .env
#
VITE_API_URL=https://api.highlyagent.io
VITE_WS_URL=wss://api.highlyagent.io/ws
# REST route prefix — backend serves at root; leave empty unless versioned
VITE_API_PREFIX=
VITE_SIMULATED=true
